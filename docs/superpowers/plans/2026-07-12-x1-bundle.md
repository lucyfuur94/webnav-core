# X1 Bundle Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Checkbox steps.

**Goal:** Close the matrix's only wrong-map gap (X1: undeclared portal overlays), the 3 grammar mismatches sharing its seam, X3 (main-landmark identity scoping), and the human-recorder requestedUrl gap. All fixes upstream, all pinned by flipping the existing `it.fails` grammar tests plus new ones.

**References:** matrix `docs/superpowers/specs/2026-07-12-structure-coverage-matrix.md` (rows 26/X1, X3; grammar mismatches in `.superpowers/../grammar-report.md` on the merged suite); grammar tests `tests/grammar/*` (3 `it.fails` cases to flip).

## Global Constraints

- Zero site tokens (guard test); evidence-only mechanisms; unknown → provisional/needsFix, never wrong; thresholds documented at definition.
- The 3 grammar `it.fails` flips happen IN the task that fixes each behavior — a flip without its fix (or vice versa) fails CI by design.
- All existing acceptance (the analytics SPA/ohrm/ae real-data) must stay green; anti-merge mutation locks intact.
- Commits by `dikshant.y`, ending `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`; `npm test` + `npx tsc --noEmit` green per task.

### Task 1: X1 — overlay by behavior + the 3 mismatch fixes (executor: opus)

**Files:** `src/explorer/draft.ts` (+`infer.ts` only if a helper genuinely belongs there), `tests/grammar/{pickers,overlays,collections,gaps}.test.ts` (flip the 3 `it.fails`), `tests/explorer/draft.test.ts`.

Locked design:
1. **Transient-subtree tracking (X1):** while walking a page's effect sequence, maintain a transient-overlay token set: a non-navigated effect's `diff.added` subtree (role-less or declared alike) ADDS its role:name tokens; a later `diff.removed` (or a navigation) clears the removed tokens. A recorded click/type whose role+name matches the transient set is gated exactly like `clickedInOverlay` (which stays — declared containers remain the stronger signal; transient tracking is the OR-arm). Value clicks inside AntD-style role-less portals then attribute to the OPENER's overlay, not the page.
2. **Reveal classification by behavior (mismatch #1):** an opener whose diff ADDED ≥1 named node is a `reveal` even when no children survive the child-role filter and value-domain folds (children may legitimately be `[]`). The current `children.length ? 'reveal' : 'mutate'` ternary (draft.ts ~760) keys on the wrong evidence — key on the added-subtree, falling back to mutate only when the diff added nothing named.
3. **REVEAL_CHILD_ROLES (mismatches #1/#3):** add `option`… NO — options are value domain and must NOT be stored as children; the mismatch resolves via rule 2 (opener still reveal, options folded out). ADD `menuitemcheckbox`, `menuitemradio` (stateful menu controls = durable repertoire). Update the pickers/gaps `it.fails` to assert: opener = reveal, children exclude the option values (flip to positive tests of the CORRECT behavior per the matrix rows, not the old mismatch text).
4. **Interior synthesis (mismatch #2):** include `columnheader` in the synthesizable set → sortable headers become mutate affordances (fold-aware: ≥2 same-shape headers still fold per the existing pass — verify the collections fixture expectation matches its matrix row).
5. Flip all 3 `it.fails` → `it` with corrected assertions; every flip must fail before its fix and pass after (state RED/GREEN per flip).

### Task 2: X3 — main-landmark identity scoping (executor: opus)

**Files:** `src/explorer/draft.ts`, tests (grammar chrome OQ1 probe flips to the fixed behavior + draft tests), acceptance additions.

Locked design: when a landing's parsed nodes contain a `main` landmark, the IDENTITY faces (dispose `normFace` inputs + SPA-split `clusterFaces` inputs) and the FINGERPRINT candidate pool scope to the main subtree's tokens (+ shell subtraction as today); no `main` declared → whole-face behavior unchanged (declared-evidence-gated, no inference). templateCore/stored faces/shadow unchanged. EVIDENCE FIRST: before coding, dump the real the analytics SPA 8001/8002 landings' landmark structure from ~/.webnav/webnav.db (is the config editor inside or outside `main`?) — report what the change will and won't merge BEFORE implementing; if the editor sits INSIDE main, the dashboards still won't merge (honest) and the task's value is the OQ1 rail-leak class — say so, don't force. Update the chrome OQ1 grammar probe to the fixed behavior; re-run all real-data acceptance.

### Task 3: human-recorder requestedUrl (executor: sonnet — user-approved capture-engine edit)

**Files:** `src/recorder/live-record.ts` (+`live.ts` only if the click event must carry the href), tests.
Same rule as the agent paths (browse.ts): when a human click lands on a link whose DECLARED href differs from the settled toUrl, record the absolutized href as `requestedUrl` (cross-page http(s) only; `#`/`javascript:` guarded). Study how assembleEffect pairs ticks and where the clicked node's SnapNode/url is available. Test via the live-record test harness.

### Task 4: acceptance + final review + merge prep (orchestrator)

Re-run full acceptance (the analytics SPA 6-session + ohrm + ae); the whole-branch review; STATUS.md entry; merge.
